package ${self.package};

${imports}

public class ${self.name} implements IXtumlType {

    // constructors
    public ${self.name}() {
${attribute_initializers}    }

    // attributes
${attributes}

    // TODO serialization

}
