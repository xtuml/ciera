package ${package};

${imports}

public class ${name} extends ${extends} {

    // supertype class
    private UniqueId ${supertype_class_name};

    // subtype class
    private UniqueId subtype;

}
